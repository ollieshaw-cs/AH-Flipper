/*

Profit:

    if 'UseProfitCostRatio' is true, it will ignore the 'MinProfit' and only show flips where the profit 
    for the flip is 'ProfitRatio' times the cost of the item

    Example:

    if UseProfitCostRatio = true and ProfitRatio = 2 -> only shows flips where the profit is 2x the cost

*/

{
    "ALLOWED_CATEGORIES" : ["weapon", "armor"],

    //"MIN_PROFIT" : "650,000",

    "Profit" : {
        "MinProfit" : "650,000",

        "UseProfitCostRatio" : true,
        "ProfitRatio" : "1.5"

    },

    "MAX_COST" : "1,000,000,000",

    "MIN_LISTINGS" : "2",
    "MIN_DAILY_VOLUME" : "8",

    "WEBHOOK_URL" : "ENTER_WEBHOOK_URL"
}